services:
  snp500:
    build: .
    volumes:
      - ./reports:/app/reports
    restart: unless-stopped
    networks:
      - ai-network

networks:
  ai-network:
    external: true
    name: docker_ai-network
